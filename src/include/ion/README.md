# `src/include/ion/`

This directory holds headers for the Ion extension that are shared across multiple translation units.

Initial refactor scaffold: the implementation is still primarily in `src/ion_extension.cpp` and will be split out
incrementally following `docs/REFACTORING_PLAN.md`.


# name: test/sql/ion_read_binary_parity_nested.test
# description: read_ion binary parity for nested data
# group: [sql]

require ion

query I
SELECT COUNT(*) FROM (
    SELECT * FROM read_ion('test/ion/nested.ion')
    EXCEPT ALL
    SELECT * FROM read_ion('test/ion/nested_binary.ion')
);
----
0

query I
SELECT COUNT(*) FROM (
    SELECT * FROM read_ion('test/ion/nested_binary.ion')
    EXCEPT ALL
    SELECT * FROM read_ion('test/ion/nested.ion')
);
----
0
